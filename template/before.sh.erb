#!/bin/bash

exec &> >(tee -a "pod.log")

export port=8080
export host=$(hostname)

source /etc/profile.d/lmod.sh
source /bin/find_host_port
source /bin/create_passwd
source /bin/save_passwd_as_secret

export password=$PASSWORD
